package com.example.book.initDataBase;
import com.example.book.model.*;
import com.example.book.model.Book;
import com.example.book.model.Genre;
import com.example.book.repository.BookRepository;
import com.example.book.repository.CopyRepository;
import com.example.book.repository.UserRepository;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

import java.util.LinkedList;
import java.util.List;
/*
@Component
public class InitDataBase implements CommandLineRunner {
    private final BookRepository bookRepository;
    private final CopyRepository copyRepository;
    private final UserRepository userRepository;

    public InitDataBase(final BookRepository bookRepository, final CopyRepository copyRepository,
                        final UserRepository userRepository) {
        this.bookRepository = bookRepository;
        this.copyRepository = copyRepository;
        this.userRepository = userRepository;
    }
    @Override
    public void run(String... args) throws Exception {
        //initAction();
        /*initAdventure();
        initComedy();
        initCartoons();
        initWesterns();
        initHorrors();
        initDramas();
        initSciFi();
        initFantasy();
        initDocumentary();
//        initCopies();


        //initUsers();
    }
    }

 */
